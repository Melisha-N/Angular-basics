import { AgecalculatePipe } from './agecalculate.pipe';

describe('AgecalculatePipe', () => {
  it('create an instance', () => {
    const pipe = new AgecalculatePipe();
    expect(pipe).toBeTruthy();
  });
});
